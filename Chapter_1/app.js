
console.log("Hi there");

// Setting the variable a to contain a value of 10
let a  = 10;

// Pringting to the console the value a stores
console.log(a);

/* This is the syntax for a multiple line comment in Javascript. The opening is a forward slash followed by an 
asterisk and the closing is an asterisk follwoed by a forward slash.
*/

// Code to generate a random number between 0 and 1
Math.random();

// Prints to the console a number between 0 and 1
console.log(Math.random());

// Prints to the console a number between 0 and 100
console.log(Math.random() * 100);

// Prints to the console a whole number between 0 and 100
console.log(Math.floor(Math.random() * 100));